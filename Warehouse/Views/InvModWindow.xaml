<Window x:Class="Warehouse.Views.InvModWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Warehouse.Views"
        mc:Ignorable="d"
                 Title="Накладная"
        Height="220"
        Width="360"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="130"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Label Grid.Row="0"
               Grid.Column="0"
               Content="Номер:"
               VerticalAlignment="Center"
               Margin="0,0,10,5"/>
        <TextBox x:Name="tbNumber"
                 Grid.Row="0"
                 Grid.Column="1"
                 Margin="0,0,0,5"
                 MinWidth="180"/>
        <Label Grid.Row="1"
               Grid.Column="0"
               Content="Тип:"
               VerticalAlignment="Center"
               Margin="0,0,10,5"/>
        <ComboBox x:Name="cbType"
                  Grid.Row="1"
                  Grid.Column="1"
                  Margin="0,0,0,5">
            <ComboBoxItem Content="Входящая"/>
            <ComboBoxItem Content="Исходящая"/>
        </ComboBox>

        <!-- Кнопки -->
        <StackPanel Grid.Row="2"
            Grid.ColumnSpan="2"
            Orientation="Horizontal"
            HorizontalAlignment="Center"
            Margin="0,10,0,0">

            <Button Content="OK"
            Width="80"
            Height="28"
            Margin="0,0,5,0"
            Click="BtnOk_Click"/>

            <Button Content="Отмена"
            Width="80"
            Height="28"
            Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
